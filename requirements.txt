pytest>=7.0

# Phase B — Dashboard prototype (docs/plans/dashboard-milestone-1.md)
streamlit>=1.35
duckdb>=0.11
pandas>=2.1
altair>=5.3

# Phase D — Representation stub (docs/plans/dashboard-milestone-1.md)
# torch CPU wheel (avoids CUDA bloat on the 5 GB container):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
scikit-learn>=1.4
# mlflow is OPT-IN to the runner (`--mlflow` flag); `--local` never imports it.
# Self-host single binary (`mlflow server`) is the intended deployment — free,
# local, reachable via the user's Cloudflare tunnel (per Q2 decision).
mlflow>=2.11

# Phase C — Dashboard on the domain route (docs/plans/dashboard-milestone-1.md)
# Read-only JSON API (FastAPI) served by uvicorn under mobile-sync.jjrdev.com
# (/api/v1/dashboard/*), passcode-gated. httpx backs the TestClient used in
# tests/test_dashboard_auth.py.
fastapi>=0.110
uvicorn>=0.29
httpx>=0.26

# Galaxy Watch / wearable compaction (ADR-021): flat parquet exports for the
# canonical 60-s windows + rollups. Optional at runtime (compact() degrades
# gracefully without it) but declared so parquet is available on a fresh clone.
pyarrow>=16
